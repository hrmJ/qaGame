{
  "name": "qagapi",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "export $(cat .env | xargs)  && ts-node-dev --respawn src/server.ts",
    "migrate": "export $(cat .env | xargs) && node-pg-migrate",
    "migrate:test:up": "export $(cat .env.test | xargs) && node-pg-migrate up",
    "migrate:test:down": "export $(cat .env.test | xargs) && node-pg-migrate down",
    "test": "export $(cat .env.test | xargs)  &&  vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "*",
    "fastify": "^4.2.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.5.2",
    "vitest": "^0.17.1"
  },
  "dependencies": {
    "@fastify/cors": "^8.0.0",
    "node-pg-migrate": "^6.2.2",
    "pg": "^8.7.3",
    "postgres": "^3.2.4"
  }
}
